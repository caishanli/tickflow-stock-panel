# ETF 真实单位净值接入（get_extras unit_net_value）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `get_extras('unit_net_value', ...)` 返回 ETF 真实单位净值（akshare `fund_etf_fund_daily_em`），替代当前 close 近似的假净值，使策略溢价率过滤真正生效。

**Architecture:** 回源侧新增 `etf_nav_service.py`（akshare 全市场净值 → 按日分区落盘 `data/etf_nav/date=YYYY-MM-DD/part.parquet`），挂进 stockdata 服务三个触发点（启动 backfill、15:35 cron、00:00 巡检）。读取侧 DataSources 加 `get_etf_nav`，StockDataClient 加 `get_etf_nav`，`jqcompat.get_extras` 与模拟盘 `api.get_extras` 共用真实净值逻辑。

**Tech Stack:** Python 3.11, FastAPI, polars, akshare（≥1.18,<2，venv 已装 1.18.64）, DuckDB/Parquet 存储。

## Global Constraints

- **策略代码不可改动**：`get_extras('unit_net_value', codes, start_date, end_date)` 语义保持——返回 DataFrame，行=日期，列=security，供策略 `nav_df.iloc[-1].values[0]` / `nav_df.iloc[-1].to_dict()` 取用。
- 数据源用 **akshare `fund_etf_fund_daily_em()`**（免费零鉴权，实测 1602 只全市场场内 ETF），不用 tushare（200 积分不够 fund_nav）、不用天天基金直连。
- 分区命名恒为 `date=YYYY-MM-DD`；文件 `part.parquet`；`symbol` 用 JQ 格式（`510300.XSHG`）。
- 落盘用**原子写**（临时文件 + rename），对齐 `mootdx_service._write_minute_partition`。
- 回源失败标的追加 `data/mootdx_sync_failures.csv`（复用 `mootdx_service._append_failure`）。
- 幂等：某日分区已存在则 `sync_etf_nav(day)` 返回 0 跳过。
- 净值收盘后（≥15:00）当日才算可回源，复用 `mootdx_service._market_closed` 口径。
- 测试从 `backend/` 目录跑：`uv run --extra dev pytest`。前端无测试。
- ruff：`uv run --extra dev ruff check app ...`（line-length 100，select E,F,I,N,UP,B,SIM,RUF，忽略 E501）。

---

### Task 1: 回源侧 `etf_nav_service.py`

**Files:**
- Create: `backend/app/services/etf_nav_service.py`
- Test: `backend/tests/quant/test_etf_nav_service.py`

**Interfaces:**
- Consumes: `mootdx_service` 的 `DATA_ROOT` 推导方式、`_append_failure`、`_partition_dates`、`_missing_days_in`、`_trade_days_up_to`、`_market_closed`（同包 import）。
- Produces:
  - `ETF_NAV_ROOT: Path` = `DATA_ROOT / "etf_nav"`。
  - `sync_etf_nav(day: date | None = None) -> int`（返回写入行数，幂等）。
  - `_missing_etf_nav_days(now=None) -> list[date]`。
  - `scan_missing_partitions()` 返回 dict 增 `"etf_nav"` 键（在 Task 1 完成）。

- [ ] **Step 1: 写失败测试 `test_etf_nav_service.py`**

```python
# backend/tests/quant/test_etf_nav_service.py
"""etf_nav_service：akshare 全市场净值 → 按日分区落盘 + 缺失日巡检。"""
import datetime as _dt
from datetime import date

import polars as pl
import pytest

from app.services import etf_nav_service as svc
from app.services import mootdx_service


def test_sync_writes_partition_and_is_idempotent(tmp_path, monkeypatch):
    monkeypatch.setenv("PARTITION_DATA_ROOT", str(tmp_path))
    import importlib
    importlib.reload(svc)

    def fake_fund_etf_fund_daily_em():
        return pl.DataFrame({
            "基金代码": ["510300", "159915", "518880"],
            "单位净值": [4.7556, 3.5860, 5.0123],
        })

    monkeypatch.setattr(svc, "_fund_etf_fund_daily_em", fake_fund_etf_fund_daily_em)
    day = date(2026, 8, 7)
    n1 = svc.sync_etf_nav(day)
    n2 = svc.sync_etf_nav(day)  # 幂等：分区已存在 → 跳过
    assert n1 == 3
    assert n2 == 0
    part = tmp_path / "etf_nav" / "date=2026-08-07" / "part.parquet"
    assert part.exists()
    df = pl.read_parquet(part)
    assert sorted(df["symbol"].to_list()) == ["159915.XSHE", "510300.XSHG", "518880.XSHG"]
    assert df["unit_nav"].dtype == pl.Float64
    assert set(df["date"].to_list()) == {"2026-08-07"}


def test_symbol_market_mapping():
    # 5/6/9 开头 → 沪市 XSHG；0/1/2/3 开头 → 深市 XSHE
    assert svc._jq_symbol("510300") == "510300.XSHG"
    assert svc._jq_symbol("159915") == "159915.XSHE"
    assert svc._jq_symbol("518880") == "518880.XSHG"


def test_missing_nav_days_respects_market_close(tmp_path, monkeypatch):
    monkeypatch.setenv("PARTITION_DATA_ROOT", str(tmp_path))
    import importlib
    importlib.reload(svc)
    # 完全空分区：收盘前不算缺失（当日无当日净值），收盘后算今天
    assert svc._missing_etf_nav_days(now=_dt.datetime(2026, 8, 7, 14, 0)) == []
    missing = svc._missing_etf_nav_days(now=_dt.datetime(2026, 8, 7, 15, 30))
    assert _dt.date(2026, 8, 7) in missing
    # 已有当日分区 → 不再缺失
    (tmp_path / "etf_nav" / "date=2026-08-07").mkdir(parents=True)
    assert _dt.date(2026, 8, 7) not in svc._missing_etf_nav_days(
        now=_dt.datetime(2026, 8, 7, 15, 30))


def test_scan_missing_partitions_has_etf_nav(tmp_path, monkeypatch):
    monkeypatch.setenv("PARTITION_DATA_ROOT", str(tmp_path))
    import importlib
    importlib.reload(svc)
    importlib.reload(mootdx_service)
    missing = mootdx_service.scan_missing_partitions()
    assert "etf_nav" in missing
    assert isinstance(missing["etf_nav"], list)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && uv run --extra dev pytest tests/quant/test_etf_nav_service.py -q`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 `etf_nav_service.py`**

```python
# backend/app/services/etf_nav_service.py
"""ETF 单位净值回源：akshare 全市场净值 → 按日分区落盘。

净值收盘后（基金公司披露）才有当日值，盘中只有昨日净值。策略用
``context.previous_date`` 取昨日，与分区日期对齐。幂等：某日分区已存在
则跳过。触发点：启动 backfill、15:35 cron、00:00 巡检。
"""
from __future__ import annotations

import datetime as _dt
import logging
import os
from datetime import date as _date
from pathlib import Path

import polars as pl

logger = logging.getLogger("app.services.etf_nav_service")

_env_root = os.getenv("PARTITION_DATA_ROOT", "").strip()
if _env_root:
    DATA_ROOT = Path(_env_root)
else:
    _repo_root = os.path.dirname(os.path.dirname(os.path.dirname(os.path.dirname(
        os.path.abspath(__file__)))))
    DATA_ROOT = Path(_repo_root) / "data"

ETF_NAV_ROOT = DATA_ROOT / "etf_nav"


def _jq_symbol(code: str) -> str:
    """6 位场内代码 → JQ 代码（5/6/9 沪市 XSHG，其余深市 XSHE）。"""
    code = str(code).strip()
    if code.startswith(("5", "6", "9")):
        return f"{code}.XSHG"
    return f"{code}.XSHE"


def _fund_etf_fund_daily_em() -> pl.DataFrame:
    """akshare 全市场场内 ETF 净值（基金代码/单位净值）。"""
    import akshare as ak
    df = ak.fund_etf_fund_daily_em()
    return pl.from_pandas(df[["基金代码", "单位净值"]])


def sync_etf_nav(day: _date | None = None) -> int:
    """同步指定日（默认今天）全市场 ETF 单位净值到按日分区。幂等。

    返回写入行数；该日分区已存在返回 0。
    """
    day = day or _date.today()
    pdir = ETF_NAV_ROOT / f"date={day}"
    part = pdir / "part.parquet"
    if part.exists():
        logger.info("etf_nav_service: %s 已存在，跳过", day)
        return 0
    raw = _fund_etf_fund_daily_em()
    if raw.is_empty():
        from app.services.mootdx_service import _append_failure
        _append_failure(f"etf_nav:{day}", "empty_akshare")
        return 0
    df = raw.with_columns(
        pl.col("基金代码").map_elements(_jq_symbol, return_dtype=pl.Utf8).alias("symbol"),
        pl.col("单位净值").cast(pl.Float64).alias("unit_nav"),
        pl.lit(day.isoformat()).alias("date"),
    ).select(["symbol", "unit_nav", "date"])
    pdir.mkdir(parents=True, exist_ok=True)
    tmp = pdir / "part.tmp"
    df.sort("symbol").write_parquet(tmp)
    tmp.rename(part)
    logger.info("etf_nav_service: 净值落盘 %d 行 → %s", df.height, part)
    return df.height


def _partition_dates() -> list[str]:
    if not ETF_NAV_ROOT.is_dir():
        return []
    return sorted(d.name[5:] for d in ETF_NAV_ROOT.iterdir()
                  if d.is_dir() and d.name.startswith("date="))


def _market_closed(now: _dt.datetime | None = None) -> bool:
    """当前是否已收盘（≥15:00）。对齐 mootdx_service 口径。"""
    from app.services.mootdx_service import _market_closed as _mc
    return _mc(now)


def _missing_etf_nav_days(now: _dt.datetime | None = None) -> list[_date]:
    """找出分区缺失的净值交易日（最新分区日期 → 今天）。

    盘中（<15:00）不把今天当缺失：当日净值尚未披露。收盘后算缺失。
    """
    from app.services.mootdx_service import _trade_days_up_to
    existing = _partition_dates()
    now = now or _dt.datetime.now()
    today = now.date()
    if not existing:
        if _market_closed(now):
            return _trade_days_up_to(today)
        return []
    latest = _date.fromisoformat(existing[-1])
    if latest >= today and not _market_closed(now):
        return []
    return [d for d in _trade_days_up_to(today) if latest < d <= today]
```

- [ ] **Step 4: 修改 `mootdx_service.py` 把 `etf_nav` 挂进巡检**

在 `scan_missing_partitions()` 的返回 dict（`mootdx_service.py:731-737`）加一行：

```python
    return {
        "kline_daily":       _missing_days_in(calendar, STOCK_DAILY_ROOT),
        "kline_etf_daily":   _missing_days_in(calendar, ETF_DAILY_ROOT),
        "kline_index_daily": _missing_days_in(calendar, INDEX_DAILY_ROOT),
        "kline_etf_minute":  _missing_days_in(calendar, ETF_MINUTE_ROOT),
        "kline_minute":      _missing_days_in(calendar, STOCK_MINUTE_ROOT),
        "etf_nav":           _missing_days_in(calendar, ETF_NAV_ROOT),
    }
```

（`ETF_NAV_ROOT` 从 `etf_nav_service` import：`from app.services.etf_nav_service import ETF_NAV_ROOT` 放在模块顶部 import 区。）

在 `backfill_missing_partitions()` 的 `min_days` 处理之后、`return result` 之前（`mootdx_service.py:537` 附近）加：

```python
    for day in missing.get("etf_nav", []):
        try:
            from app.services import etf_nav_service
            etf_nav_service.sync_etf_nav(day)
            result["etf_nav_days"].append(str(day))
        except Exception as e:  # noqa: BLE001
            result["errors"].append(f"etf_nav {day}: {e}")
```

并把 `result` 初始 dict（`mootdx_service.py:508-511`）加 `"etf_nav_days": []`。

- [ ] **Step 5: 运行全部测试确认通过**

Run: `cd backend && uv run --extra dev pytest tests/quant/test_etf_nav_service.py -q`
Expected: PASS（4 passed）

- [ ] **Step 6: 提交**

```bash
git add backend/app/services/etf_nav_service.py backend/app/services/mootdx_service.py backend/tests/quant/test_etf_nav_service.py
git commit -m "feat(stockdata): ETF 真实净值回源 service + 00:00 巡检补漏"
```

---

### Task 2: 读取侧 client + handler + get_extras

**Files:**
- Modify: `backend/app/services/stockdata/sources.py`
- Modify: `backend/app/services/stockdata/handlers.py`
- Modify: `backend/app/quant/datasource/network_client.py`
- Modify: `backend/app/quant/jqcompat.py`（`get_extras` 的 `unit_net_value` 分支）
- Modify: `backend/app/quant/jqengine/engine/jq/api.py`（`get_extras` 的 `unit_net_value` 分支）
- Test: `backend/tests/quant/test_etf_nav_client.py`

**Interfaces:**
- Consumes:
  - `DataSources._scan_partitions(subdir, day_lo, day_hi, symbols, cols) -> pl.DataFrame`（Task 0 已有）。
  - `DataManager.client`（StockDataClient 实例，manager.py:176-179 已有）。
  - `network_client.StockDataClient._request(method, params, retry=3)`（已有）。
  - `handlers._norm_codes(security) -> list[str]`（已有）。
  - `sources._tf_symbol(code) -> str`（已有）。
- Produces:
  - `DataSources.get_etf_nav(codes: list[str], date: str | None = None) -> pl.DataFrame`（cols: `symbol/unit_nav/date`）。
  - `handlers.HANDLERS["get_etf_nav"]`。
  - `StockDataClient.get_etf_nav(codes, date=None) -> dict[str, pd.DataFrame]`（key = 平台代码，值 = 含 unit_nav 的 DataFrame）。
  - `_get_etf_nav_df(securities, start_date, end_date) -> pd.DataFrame`（行=日期、列=security，真实净值）。
  - `jqcompat.get_extras` / `engine/jq/api.py.get_extras` 的 `unit_net_value` 分支改用 `_get_etf_nav_df`。

- [ ] **Step 1: 写失败测试 `test_etf_nav_client.py`**

```python
# backend/tests/quant/test_etf_nav_client.py
"""读取侧：DataSources.get_etf_nav + StockDataClient 往返 + get_extras 真实净值。"""
import datetime as _dt
from datetime import date

import pandas as pd
import polars as pl
import pytest

from app.quant.jqcompat import get_extras as jq_get_extras
from app.quant.jqengine.engine.jq import api as sim_api
from app.services.stockdata.sources import DataSources


def _write_nav_partition(root, day, rows):
    pdir = root / "etf_nav" / f"date={day}"
    pdir.mkdir(parents=True, exist_ok=True)
    df = pl.DataFrame(rows)
    df.write_parquet(pdir / "part.parquet")


def test_datasources_get_etf_nav(tmp_path):
    _write_nav_partition(tmp_path, "2026-08-07", {
        "symbol": ["510300.XSHG", "159915.XSHE"],
        "unit_nav": [4.7556, 3.5860],
        "date": ["2026-08-07", "2026-08-07"],
    })
    ds = DataSources(data_root=str(tmp_path))
    df = ds.get_etf_nav(["510300.XSHG"], "2026-08-07")
    assert df is not None and df.height == 1
    row = df.filter(pl.col("symbol") == "510300.XSHG").to_dicts()[0]
    assert abs(row["unit_nav"] - 4.7556) < 1e-9


def test_datasources_get_etf_nav_latest_partition(tmp_path):
    _write_nav_partition(tmp_path, "2026-08-06", {
        "symbol": ["510300.XSHG"], "unit_nav": [4.7111],
        "date": ["2026-08-06"],
    })
    ds = DataSources(data_root=str(tmp_path))
    df = ds.get_etf_nav(["510300.XSHG"], None)  # 无 date → 最新分区
    assert df is not None and df.height == 1
    assert df["date"].to_list() == ["2026-08-06"]
```

`get_extras` 真实净值测试依赖 mock。看当前 `_get_etf_nav_df` 取数入口再定 mock 目标——实现用 `manager.client.get_etf_nav`，测试用最小 fake manager：

```python
class _FakeNavClient:
    def __init__(self, navs):
        self.navs = navs  # {code: {date_str: unit_nav}}

    def get_etf_nav(self, codes, date=None):
        out = {}
        for c in codes:
            rows = self.navs.get(c, {})
            idx = sorted(rows)
            if date is not None:
                idx = [d for d in idx if d <= date]
            out[c] = pd.DataFrame({
                "date": idx,
                "unit_nav": [rows[d] for d in idx],
            })
        return out


class _FakeNavManager:
    def __init__(self, navs):
        self.client = _FakeNavClient(navs)


def test_sim_get_extras_returns_real_nav(monkeypatch):
    navs = {"510300.XSHG": {"2026-08-07": 4.7556}}
    mgr = _FakeNavManager(navs)
    sim_api._reset(mgr, 0.0003, 0.001, 10000.0)
    df = sim_api.get_extras("unit_net_value", ["510300.XSHG"],
                            "2026-08-07", "2026-08-07")
    assert not df.empty
    assert list(df.columns) == ["510300.XSHG"]
    assert abs(df["510300.XSHG"].iloc[-1] - 4.7556) < 1e-9


def test_sim_get_extras_missing_nav_returns_empty(monkeypatch):
    mgr = _FakeNavManager({})
    sim_api._reset(mgr, 0.0003, 0.001, 10000.0)
    df = sim_api.get_extras("unit_net_value", ["510300.XSHG"],
                            "2026-08-07", "2026-08-07")
    assert df.empty


def test_unsupported_field_still_empty():
    sim_api._reset(_FakeNavManager({}), 0.0003, 0.001, 10000.0)
    df = sim_api.get_extras("acc_net_value", ["510300.XSHG"])
    assert df.empty
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && uv run --extra dev pytest tests/quant/test_etf_nav_client.py -q`
Expected: FAIL

- [ ] **Step 3: `sources.py` 加 `get_etf_nav`**

在 `DataSources.get_daily`（sources.py:340）之后加：

```python
    def get_etf_nav(self, codes: list[str], date: str | None = None) -> pl.DataFrame:
        """读 etf_nav 分区（date 给定用该日，None 用最新分区）。"""
        def _load():
            syms = {_tf_symbol(c) for c in codes}
            cols = ["symbol", "unit_nav", "date"]
            lo = hi = date
            if date is None:
                parts_root = os.path.join(self.data_root, "etf_nav")
                dates = sorted(
                    d[5:] for d in os.listdir(parts_root)
                    if d.startswith("date=")) if os.path.isdir(parts_root) else []
                if not dates:
                    return pl.DataFrame()
                hi = dates[-1]
            return self._scan_partitions("etf_nav", lo, hi, syms, cols)
        key = f"nav:{','.join(sorted(codes))}:{date or 'latest'}"
        return self.get_or_fetch(key, _HIST_TTL, _load)
```

- [ ] **Step 4: `handlers.py` 加 `h_get_etf_nav`**

```python
def h_get_etf_nav(p, s: DataSources):
    codes = _norm_codes(p["security"])
    return "parquet", s.get_etf_nav(codes, p.get("date"))
```

注册进 `HANDLERS`（handlers.py:107-121）：

```python
    "get_etf_nav": h_get_etf_nav,
```

- [ ] **Step 5: `network_client.py` 加 `get_etf_nav`**

在 `StockDataClient.get_adj_factors`（network_client.py:159）之后加：

```python
    def get_etf_nav(self, codes, date=None) -> dict[str, pd.DataFrame]:
        resp = self._request("get_etf_nav", {
            "security": codes, "date": date,
        })
        return self._parquet_to_dict(resp)
```

- [ ] **Step 6: `jqcompat.py` 与 `engine/jq/api.py` 的 get_extras 共用真实净值**

两处 `get_extras` 的 `unit_net_value` 分支改为：

```python
    if field != "unit_net_value":
        _emit_sink("warn", f"[get_extras] field={field} 未实现，返回空 DataFrame")
        return pd.DataFrame()
    codes = [securities] if isinstance(securities, str) else list(securities)
    end_dt = pd.Timestamp(end_date) if end_date is not None else (
        getattr(_state.get("ctx"), "current_dt", None) or pd.Timestamp.now())
    navs = _get_etf_nav_df(codes, end_dt.date())
    if isinstance(securities, str):
        return navs[[codes[0]]] if codes[0] in navs.columns else pd.DataFrame()
    return navs
```

> `end_dt` 兜底各有环境：模拟盘 `engine/jq/api.py` 用 `_state.get("ctx").current_dt`；
> `jqcompat.py` 用 `Environment.get_instance().trading_dt`。`end_date is None` 时各自兜底。

并新增共享函数（两文件各自实现，逻辑一致）：

```python
def _get_etf_nav_df(codes: list[str], end_date) -> pd.DataFrame:
    """从 StockDataClient 拉真实单位净值：end_date 精确优先，回退最近可用日。

    返回 DataFrame：行=日期，列=security。无数据返回空 DataFrame。
    """
    import pandas as _pd
    mgr = _get_nav_manager()
    if mgr is None or not hasattr(mgr, "client"):
        _emit_sink("warn", "[get_extras] 无可用 DataManager，返回空")
        return pd.DataFrame()
    raw = mgr.client.get_etf_nav(codes, str(end_date))
    if not raw:
        return pd.DataFrame()
    out = {}
    for code in codes:
        df = raw.get(code)
        if df is None or df.empty:
            continue
        out[code] = pd.Series(
            df["unit_nav"].values,
            index=pd.to_datetime(df["date"]),
        )
    if not out:
        return pd.DataFrame()
    result = pd.DataFrame(out).sort_index()
    return result[result.index <= pd.Timestamp(end_date)]
```

> **两个环境取 manager 的方式（已确认）**：
>
> 1. 模拟盘 `engine/jq/api.py`：模块已有 `_state["manager"]`（`_reset` 时写入，
>    `api.py:711`），取 `mgr = _state.get("manager")`。
> 2. 回测 `jqcompat.py`：rqalpha `Environment.get_instance().data_source` 是
>    `QuantRQAlphaDataSource`（`jqcompat.py:1138`），持有 `self._dm`（DataManager），
>    取 `mgr = Environment.get_instance().data_source._dm`。
>
> 每个文件定义自己的 `_get_nav_manager()`：
>
> ```python
> # jqcompat.py 版
> def _get_nav_manager():
>     try:
>         env = Environment.get_instance()
>         ds = getattr(env, "data_source", None)
>         return getattr(ds, "_dm", None)
>     except Exception:
>         return None
> ```
>
> ```python
> # engine/jq/api.py 版
> def _get_nav_manager():
>     return _state.get("manager")
> ```
>
> 注意：两文件各自实现 `_get_nav_manager` 与 `_get_etf_nav_df`，逻辑一致
> （DRY 受限于文件边界，不跨模块 import）。

- [ ] **Step 7: 运行全部测试确认通过**

Run: `cd backend && uv run --extra dev pytest tests/quant/test_etf_nav_client.py tests/quant/test_engine_order_rules.py -q`
Expected: PASS（两文件全绿）

- [ ] **Step 8: 提交**

```bash
git add backend/app/services/stockdata/sources.py backend/app/services/stockdata/handlers.py backend/app/quant/datasource/network_client.py backend/app/quant/jqcompat.py backend/app/quant/jqengine/engine/jq/api.py backend/tests/quant/test_etf_nav_client.py
git commit -m "feat(quant): get_extras unit_net_value 改用真实 ETF 净值（akshare）"
```

---

### Task 3: 15:35 cron 与启动 backfill 接线 + 依赖

**Files:**
- Modify: `backend/app/services/stockdata/scheduler.py`（`_run_sync` 末尾）
- Modify: `backend/app/services/mootdx_service.py`（`backfill_to_now` 末尾）
- Modify: `backend/pyproject.toml`（加 akshare 依赖）
- Test: `backend/tests/quant/test_etf_nav_service.py`（追加用例）

**Interfaces:**
- Consumes: `etf_nav_service.sync_etf_nav`、`etf_nav_service._missing_etf_nav_days`、`mootdx_service.backfill_to_now` 的 `result` dict。
- Produces: `backfill_to_now()` 返回 dict 含 `etf_nav_days` 与 `missing["etf_nav"]`。

- [ ] **Step 1: 写失败测试（backfill 汇总含 etf_nav）**

在 `test_etf_nav_service.py` 追加：

```python
def test_backfill_to_now_includes_etf_nav(tmp_path, monkeypatch):
    monkeypatch.setenv("PARTITION_DATA_ROOT", str(tmp_path))
    import importlib
    importlib.reload(svc)
    importlib.reload(mootdx_service)

    def fake_sync(day=None):
        return 3

    def fake_missing(now=None):
        return [_dt.date(2026, 8, 7)]

    monkeypatch.setattr(svc, "sync_etf_nav", fake_sync)
    monkeypatch.setattr(svc, "_missing_etf_nav_days", fake_missing)
    res = mootdx_service.backfill_to_now()
    assert "etf_nav_days" in res
    assert "etf_nav" in res.get("missing", {})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && uv run --extra dev pytest tests/quant/test_etf_nav_service.py -q`
Expected: FAIL（`backfill_to_now` 无 etf_nav 键）

- [ ] **Step 3: `backfill_to_now()` 末尾接线**

`mootdx_service.py` `backfill_to_now()` 内、`result["missing"]` 赋值处（约 :1153）加：

```python
        "etf_nav":          {"latest": ETF_NAV_ROOT 的分区最新 or None,
                             "empty": not etf_nav_days, "missing": bool(_missing_etf_nav_days())},
```

并在函数尾部（返回前）加：

```python
    from app.services import etf_nav_service
    for day in etf_nav_service._missing_etf_nav_days():
        try:
            etf_nav_service.sync_etf_nav(day)
            result["etf_nav_days"].append(str(day))
        except Exception as e:  # noqa: BLE001
            result["errors"].append(f"etf_nav {day}: {e}")
```

（`result` 初始 dict 已含 `"etf_nav_days": []`——Task 1 Step 4 已加。）

- [ ] **Step 4: `scheduler._run_sync` 末尾接线**

`scheduler.py` `_run_sync`（:33-48）在 `stock = mootdx_service.sync_stock_minute(...)` 之后加：

```python
            from app.services import etf_nav_service
            nav = etf_nav_service.sync_etf_nav()
```

并把 `_scheduler_state["sync_result"]` 加 `"nav": nav`：

```python
                _scheduler_state["sync_result"] = {
                    "minute_rows": minutes, "adj": adj,
                    "stock_minute_rows": stock, "nav_rows": nav}
```

- [ ] **Step 5: `pyproject.toml` 加 akshare 依赖**

`backend/pyproject.toml` 的 `[project] dependencies` 加：

```toml
    "akshare>=1.18,<2",
```

- [ ] **Step 6: 运行全部相关测试**

Run: `cd backend && uv run --extra dev pytest tests/quant/test_etf_nav_service.py tests/quant/test_etf_nav_client.py tests/quant/test_engine_order_rules.py tests/quant/test_fix_datamanager.py -q`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add backend/app/services/stockdata/scheduler.py backend/app/services/mootdx_service.py backend/pyproject.toml backend/tests/quant/test_etf_nav_service.py
git commit -m "feat(stockdata): etf_nav 挂 15:35 cron + 启动 backfill，依赖加 akshare"
```

---

## 验收

- [ ] `cd backend && uv run --extra dev pytest tests/quant/test_etf_nav_service.py tests/quant/test_etf_nav_client.py tests/quant/test_engine_order_rules.py -q` 全绿。
- [ ] `cd backend && uv run --extra dev ruff check app/services/etf_nav_service.py app/services/stockdata/sources.py app/services/stockdata/handlers.py app/quant/datasource/network_client.py app/quant/jqcompat.py app/quant/jqengine/engine/jq/api.py` 无新增错误（相对 HEAD 对比）。
- [ ] `get_extras('unit_net_value', '510300.XSHG', '2026-08-07', '2026-08-07')` 返回真实净值 4.7556（可手动脚本验证，mock 已覆盖）。
