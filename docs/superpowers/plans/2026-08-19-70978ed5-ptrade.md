# 70978ed5 策略 PTrade 原生版实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把策略库中的 JQ 版 70978ed5（五福v5.4 单持仓）严格按 `guojin-ptrade-api.docx` 改写成 PTrade 原生版，存回策略库，并补齐本地/rqalpha 双引擎支持与对齐门禁验证。

**Architecture:** 参照 `backend/tests/fixtures/dual_v54/wufu-v5.4-dual-adapt.ptrade.py` 的原生化先例：策略删掉全部 jq 痕迹，正文直调 docx 原生 PTrade API（`.SS/.SZ` 码、`run_daily(context, func, time)`、`before_trading_start`/`after_trading_end`/`handle_data`、`get_history`/`get_price`/`get_stock_status`/`check_limit`/`get_stock_name`/`get_etf_list`/`get_market_list`/`get_market_detail`/`order`/`get_position`/`get_positions`/`context.portfolio.cash`/`context.portfolio.portfolio_value`/`context.blotter.current_dt`/`context.previous_date`）。本地引擎（`ptrade_api.py`）与 rqalpha 引擎（`ptradecompat.py`）补齐 docx 原生函数并注入 loader 命名空间。新策略经 store 导入注册为策略库条目，前端回测路由到 ptradecompat 引擎。

**Tech Stack:** Python 3.11，pandas/numpy，pytest（asyncio_mode=auto，`uv run --extra dev`），rqalpha 6.2.1（回测对齐），ruff/mypy 代码质量门禁。

## Global Constraints

- 代码域：PTrade（`.SS`/`.SZ`），禁止出现 `.XSHG`/`.XSHE` 于策略可执行代码中（注释允许）。
- 禁止出现 jq 独有 API：`jqdata`/`get_current_data`/`attribute_history`/`record(`/`set_option`/`set_order_cost`/`get_all_securities`/`get_security_info`/`get_security_name(`（jq 版）。
- 禁止出现 jq→ptrade 转换层函数名：`_pt(`/`_cd`/`_set_last_data`/`_BarUnit`/`_safe_log`/`_warn(`/`_as_series_values`/`_positions_map`/`_pos_amount`/`_pos_avail`/`_pos_cost`/`_pos_price`/`_get_total_value`/`_get_available_cash`/`_update_universe`/`_current_price(`。
- 日志只用 `log.info/warning/error/debug`（无 `log.warn`），不用 f-string 拼接（用 `%` 格式化）。
- 调度：`run_daily` 任务总数 ≤5（本策略固定 4 个）；13:10 三个任务同点、按注册顺序执行。
- 引擎格式保证：`get_history`/`get_price` 多标的宽表（index=datetime, columns=PTrade 码），单标的列=字段名；`get_positions()` 恒 `{PTrade码: Position}`（amount/enable_amount/cost_basis/last_sale_price）；`handle_data` 的 `data` 恒 `{PTrade码: obj}`（close/price/volume/money）。
- 对齐门禁硬指标：JQ vs PTrade 同窗口回测逐笔成交完全一致 + 逐日净值零差（`max abs diff == 0`）。
- 命令统一用 `uv run --extra dev`（后端），`pnpm lint`/`pnpm build`（前端不动，除非改动前端）。

---

## File Structure

- `backend/tests/fixtures/70978ed5_ptrade/70978ed5.py` — JQ 原版拷贝（从 `data/quant_strategies/70978ed5.py` 复制，对齐门禁 JQ 侧源，保证 CI 不依赖 gitignored 的 `data/`）。
- `backend/tests/fixtures/70978ed5_ptrade/70978ed5.ptrade.py` — PTrade 原生版（源真相，本计划核心交付物）。
- `backend/app/quant/ptradeengine/ptrade_api.py` — 本地引擎（模拟盘 runner）补 docx 原生函数。
- `backend/app/quant/ptradecompat.py` — rqalpha 引擎（回测）补 docx 原生函数。
- `backend/app/quant/ptradeengine/ptrade_loader.py` — loader 命名空间注入新增函数。
- `backend/tests/quant/test_ptrade_70978ed5_file.py` — 编译 + 无 jq API 清单 + 调度断言。
- `backend/tests/quant/test_70978ed5_ptrade_alignment.py` — 对齐门禁（integration）。
- `backend/app/quant/strategies/store.py` — 仅调用其公开 `import_strategy(name, code)`，不改。
- `docs/superpowers/plans/2026-08-19-70978ed5-ptrade.md` — 本计划。

---

### Task 1: 准备 fixture 目录与 JQ 源拷贝 + 编译/API 清单测试

**Files:**
- Create: `backend/tests/fixtures/70978ed5_ptrade/70978ed5.py`
- Create: `backend/tests/fixtures/70978ed5_ptrade/70978ed5.ptrade.py`（空壳占位，Task 3 填充完整内容）
- Create: `backend/tests/quant/test_ptrade_70978ed5_file.py`

**Interfaces:**
- Consumes: 现有 fixture 模式（`backend/tests/quant/test_ptrade_strategy_file.py`）。
- Produces: `PT_FIXTURE_DIR` 常量、`PT_STRATEGY` 路径，供 Task 5 对齐门禁引用。

- [ ] **Step 1: 创建 fixture 目录并拷贝 JQ 源**

```bash
mkdir -p backend/tests/fixtures/70978ed5_ptrade
cp data/quant_strategies/70978ed5.py backend/tests/fixtures/70978ed5_ptrade/70978ed5.py
```

验证：`wc -l backend/tests/fixtures/70978ed5_ptrade/70978ed5.py` 输出 `1398`。

- [ ] **Step 2: 写占位 PTrade 策略文件（Task 3 覆盖）**

```python
# -*- coding: utf-8 -*-
# 占位，Task 3 填充
def initialize(context):
    pass

def handle_data(context, data):
    pass
```

- [ ] **Step 3: 写编译 + 无 jq API 清单 + 调度断言测试**

创建 `backend/tests/quant/test_ptrade_70978ed5_file.py`：

```python
"""70978ed5 ptrade 原生版文件验证：编译 + 无聚宽独有调用 + 调度断言。"""
import py_compile
from pathlib import Path

PT_FIXTURE_DIR = Path(__file__).parent.parent / "fixtures" / "70978ed5_ptrade"
PT_STRATEGY = PT_FIXTURE_DIR / "70978ed5.ptrade.py"
JQ_SOURCE = PT_FIXTURE_DIR / "70978ed5.py"

_JQ_ONLY = [
    "jqdata", "get_current_data", "attribute_history", "record(",
    "set_option", "set_order_cost", "get_all_securities", "get_security_info",
    "get_security_name(", "every_bar",
]

_CONV_LAYER = [
    "_pt(", "_cd(", "_cd_field", "_set_last_data", "_BarUnit", "_safe_log",
    "_warn(", "_debug(", "_as_series_values", "_positions_map",
    "_get_position(", "_pos_amount", "_pos_avail", "_pos_cost", "_pos_price",
    "_get_total_value", "_get_available_cash", "_update_universe", "_current_price(",
    "_get_today_volume(",
]


def _code_lines():
    out = []
    for line in PT_STRATEGY.read_text(encoding="utf-8").splitlines():
        stripped = line.lstrip()
        if stripped.startswith("#"):
            continue
        out.append(line)
    return out


def test_jq_source_exists():
    assert JQ_SOURCE.exists()
    assert "from jqdata import *" in JQ_SOURCE.read_text(encoding="utf-8")

def test_ptrade_compiles():
    assert PT_STRATEGY.exists()
    py_compile.compile(str(PT_STRATEGY), doraise=True)

def test_no_jq_apis():
    for line in _code_lines():
        for kw in _JQ_ONLY:
            assert kw not in line, kw

def test_no_conversion_layer():
    for line in _code_lines():
        for kw in _CONV_LAYER:
            assert kw not in line, kw

def test_no_xshg_codes_in_code():
    for line in _code_lines():
        assert ".XSHG" not in line and ".XSHE" not in line, line

def test_uses_docx_apis():
    src = PT_STRATEGY.read_text(encoding="utf-8")
    for api in ["get_history", "get_price", "get_stock_status", "get_stock_name",
                "get_etf_list", "get_market_list", "get_market_detail",
                "run_daily(context", "before_trading_start", "after_trading_end",
                "handle_data", "context.blotter.current_dt", "context.previous_date"]:
        assert api in src, api

def test_run_daily_count_within_limit():
    import re
    src = PT_STRATEGY.read_text(encoding="utf-8")
    count = len(re.findall(r"run_daily\s*\(", src))
    assert 1 <= count <= 5, f"run_daily 总数 {count} 超出 PTrade 限制 5"

def test_thirteen_ten_tasks_same_time():
    src = PT_STRATEGY.read_text(encoding="utf-8")
    assert src.count("time='13:10'") == 3, "13:10 同点任务应为 3 个"
    assert src.count("time='09:40'") == 1
    for fn in ["afternoon_routine", "sell_routine", "buy_routine"]:
        assert ("run_daily(context, %s, time='13:10')" % fn) in src, fn

def test_no_fstring_log():
    import re
    for line in _code_lines():
        assert not re.search(r"\bf([\"'])", line), f"策略不应使用 f-string: {line}"

def test_no_log_warn():
    src = PT_STRATEGY.read_text(encoding="utf-8")
    assert "log.warn(" not in src
```

- [ ] **Step 4: 运行测试，确认除占位文件外的断言失败**

Run: `cd backend && uv run --extra dev pytest tests/quant/test_ptrade_70978ed5_file.py -v`
Expected: `test_ptrade_compiles` 通过（占位可编译），`test_jq_source_exists` 通过，其余关于 PT 文件内容的断言失败（占位内容不满足）。

- [ ] **Step 5: 提交**

```bash
git add backend/tests/fixtures/70978ed5_ptrade/ backend/tests/quant/test_ptrade_70978ed5_file.py
git commit -m "test: 70978ed5 ptrade 文件验证测试（编译/无 jq API/调度断言）+ JQ 源 fixture 拷贝"
```

---

### Task 2: 引擎补齐 docx 原生函数（ptrade_api.py + ptradecompat.py + loader）

**Files:**
- Modify: `backend/app/quant/ptradeengine/ptrade_api.py`（本地引擎）
- Modify: `backend/app/quant/ptradecompat.py`（rqalpha 引擎）
- Modify: `backend/app/quant/ptradeengine/ptrade_loader.py`（命名空间注入）
- Test: `backend/tests/quant/test_ptrade_70978ed5_engine.py`

**Interfaces:**
- Consumes: 现有 `ptrade_api._state` / `ptradecompat._DAILY_AT` 等模块状态。
- Produces: 以下 docx 原生函数在两个引擎中可用，并注入 loader 命名空间：
  - `get_price(security, start_date=None, end_date=None, frequency='1d', fields=None, fq='pre', count=None, is_dict=False)` → 宽表 DataFrame（index=datetime, columns=PTrade 码；单标的单字段列=字段名）；分钟频率支持 start_date+end_date 或 count+end_date，日线支持 count+end_date。
  - `check_limit(security, query_date=None)` → `{PTrade码: int}`（-2 触板跌停/-1 跌停/0 平/1 涨停/2 触板涨停）。
  - `get_stock_info(stocks, field=None)` → `{码: {stock_name, listed_date, de_listed_date}}`。
  - `get_snapshot(security)` → `{码: {up_px, down_px, last_px, preclose_px, high_px, low_px, business_balance, volume, amount}}`（回测回退日线/分钟数据）。
  - `order_target(security, amount, limit_price=None)` / `order_target_value(security, value, limit_price=None)` → order id 或 None。
  - `get_all_trades_days(date=None)` → `list[datetime.date]`（date 之前全部交易日）。
  - `get_trading_day_by_date(query_date, day=0)` → `datetime.date`。
  - `get_etf_info(stocks)` → `{码: 名称}`。

- [ ] **Step 1: 写失败测试（本地引擎 ptrade_api）**

创建 `backend/tests/quant/test_ptrade_70978ed5_engine.py`：

```python
"""ptrade 引擎补齐的 docx 原生函数测试（本地 ptrade_api + rqalpha ptradecompat）。"""
import types
from datetime import datetime

import numpy as np
import pandas as pd
import pytest

from app.quant.ptradeengine import ptrade_api


class _FakeMgr:
    def __init__(self):
        self._daily_mem = {}
        self._minute_mem = {}

    def fetch(self, name, *a, **kw):
        if name == "get_daily":
            idx = pd.date_range("2026-06-01", "2026-07-10", freq="B")
            return pd.DataFrame({"close": np.linspace(10, 11, len(idx)),
                                 "volume": np.full(len(idx), 1000.0),
                                 "money": np.linspace(1e6, 1.1e6, len(idx))},
                                index=idx)
        if name == "get_etf_list":
            return ["510300.SH", "159915.SZ"]
        return None

    def get_daily_money_cached(self, codes, end_date, count):
        idx = pd.date_range("2026-07-06", "2026-07-10", freq="B")
        rows = []
        for c in codes:
            for t in idx:
                rows.append({"time": t, "code": c, "money": 1.0e6})
        return pd.DataFrame(rows)

    def get_minute_price_at(self, code, dt):
        return 10.5

    def get_minute(self, code, date_str, limit=None):
        idx = pd.date_range("2026-07-10 09:31", periods=100, freq="min")
        return pd.DataFrame({"close": np.full(100, 10.5),
                             "volume": np.full(100, 100.0)}, index=idx)


@pytest.fixture
def pt_ctx():
    ctx = ptrade_api._reset(_FakeMgr(), 0.0001, 0.0001, 100000.0)
    ctx.current_dt = datetime(2026, 7, 10, 14, 0)
    return ctx


def test_get_price_daily_wide(pt_ctx):
    df = ptrade_api.get_price(
        ["510300.SS", "159915.SZ"], end_date="2026-07-09", count=5,
        frequency="1d", fields=["close"])
    assert isinstance(df, pd.DataFrame)
    assert list(df.columns) == ["510300.SS", "159915.SZ"]
    assert len(df) == 5

def test_get_price_single_field_col(pt_ctx):
    df = ptrade_api.get_price(
        "510300.SS", end_date="2026-07-09", count=5, frequency="1d", fields=["close"])
    assert list(df.columns) == ["close"] or list(df.columns) == ["510300.SS"]

def test_get_price_minute_volume(pt_ctx):
    df = ptrade_api.get_price(
        ["510300.SS"], start_date="2026-07-10 09:31", end_date="2026-07-10 14:00",
        frequency="1m", fields=["volume"])
    assert isinstance(df, pd.DataFrame)
    assert "510300.SS" in df.columns or "volume" in df.columns

def test_check_limit_returns_dict(pt_ctx):
    res = ptrade_api.check_limit("510300.SS")
    assert isinstance(res, dict)
    assert "510300.SS" in res
    assert res["510300.SS"] in (-2, -1, 0, 1, 2)

def test_get_stock_info(pt_ctx):
    res = ptrade_api.get_stock_info(["510300.SS", "159915.SZ"])
    assert isinstance(res, dict)
    assert "510300.SS" in res

def test_order_target_value(pt_ctx):
    ok = ptrade_api.order_target_value("510300.SS", 50000)
    assert ok is True or ok is False

def test_get_all_trades_days(pt_ctx):
    days = ptrade_api.get_all_trades_days()
    assert isinstance(days, list) and len(days) > 0
    assert hasattr(days[0], "year")

def test_get_trading_day_by_date(pt_ctx):
    d = ptrade_api.get_trading_day_by_date("2026-07-10", day=-1)
    assert hasattr(d, "year")

def test_get_etf_info(pt_ctx):
    info = ptrade_api.get_etf_info(["510300.SS", "159915.SZ"])
    assert isinstance(info, dict)
    for v in info.values():
        assert isinstance(v, str)
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd backend && uv run --extra dev pytest tests/quant/test_ptrade_70978ed5_engine.py -v`
Expected: FAIL（`AttributeError: module '...ptrade_api' has no attribute 'get_price'` 等）。

- [ ] **Step 3: 实现本地引擎 ptrade_api.py 补齐函数**

在 `backend/app/quant/ptradeengine/ptrade_api.py` 末尾（`build_data_snapshot` 之前）追加：

```python
def get_price(security, start_date=None, end_date=None, frequency="1d",
              fields=None, fq="pre", count=None, is_dict=False):
    """docx 原生 get_price：复用 get_history 口径。返回宽表 DataFrame。"""
    fields = fields or ["close"]
    if isinstance(fields, str):
        fields = [fields]
    if isinstance(security, str):
        codes = [security]
    else:
        codes = list(security)
    field_out = {}
    for f in fields:
        df = get_history(count, frequency, f, security_list=codes, include=False, fq=fq)
        if df is None or df.empty:
            continue
        field_out[f] = df
    if not field_out:
        return pd.DataFrame()
    if len(fields) == 1:
        df = field_out[fields[0]]
        if len(codes) == 1 and len(df.columns) == 1:
            df = df.copy()
            df.columns = [fields[0]]
        return df
    # 多字段：返回 {field: DataFrame} 的 dict（docx py3.5 panel 变体简化）
    return field_out


def check_limit(security, query_date=None):
    """docx check_limit：{码: int}，-2 触板跌停/-1 跌停/0 平/1 涨停/2 触板涨停。
    本地引擎用日线 high_limit/low_limit 字段 + 最新价推导。"""
    codes = [security] if isinstance(security, str) else list(security)
    out = {}
    for c in codes:
        out[c] = 0
        try:
            df = get_history(1, "1d", ["high_limit", "low_limit", "close"],
                             security_list=c, include=True)
            if df is None or df.empty:
                continue
            last = float(df["close"].iloc[-1])
            high = float(df["high_limit"].iloc[-1]) if "high_limit" in df.columns else 0
            low = float(df["low_limit"].iloc[-1]) if "low_limit" in df.columns else 0
            if high and last >= high:
                out[c] = 1
            elif low and last <= low:
                out[c] = -1
        except Exception:
            continue
    return out


def get_stock_info(stocks, field=None):
    """docx get_stock_info：{码: {stock_name, listed_date, de_listed_date}}。"""
    codes = [stocks] if isinstance(stocks, str) else list(stocks)
    fields = ["stock_name", "listed_date", "de_listed_date"] if field is None else (
        [field] if isinstance(field, str) else list(field))
    out = {}
    for c in codes:
        out[c] = {f: ("" if f != "stock_name" else _resolve_name(c)) for f in fields}
    return out


def get_snapshot(security):
    """docx get_snapshot：{码: {up_px, down_px, last_px, ...}}。回测回退日线字段。"""
    codes = [security] if isinstance(security, str) else list(security)
    out = {}
    for c in codes:
        snap = {"last_px": 0.0, "up_px": 0.0, "down_px": 0.0,
                "preclose_px": 0.0, "high_px": 0.0, "low_px": 0.0,
                "business_balance": 0.0, "volume": 0, "amount": 0}
        try:
            df = get_history(1, "1d", ["close", "high_limit", "low_limit", "preclose"],
                             security_list=c, include=True)
            if df is not None and not df.empty:
                snap["last_px"] = float(df["close"].iloc[-1])
                if "high_limit" in df.columns:
                    snap["up_px"] = float(df["high_limit"].iloc[-1])
                if "low_limit" in df.columns:
                    snap["down_px"] = float(df["low_limit"].iloc[-1])
                if "preclose" in df.columns:
                    snap["preclose_px"] = float(df["preclose"].iloc[-1])
        except Exception:
            pass
        out[c] = snap
    return out


def order_target(security, amount, limit_price=None):
    """docx order_target：调整持仓到目标数量。"""
    cur = get_position(security).amount
    diff = int(amount) - int(cur)
    if diff == 0:
        return True
    return order(security, diff) if diff > 0 else order(security, -min(abs(diff), get_position(security).enable_amount))


def order_target_value(security, value, limit_price=None):
    """docx order_target_value：调整持仓到目标市值。本地引擎用 order_value 近似（差异仅整手尾差）。"""
    p = _state["ctx"].portfolio
    price = _live_price(security)
    if price == 0:
        return False
    cur_val = get_position(security).amount * price
    diff = float(value) - cur_val
    if abs(diff) < price * 100:
        return True
    return order_value(security, diff)


def get_all_trades_days(date=None):
    """docx get_all_trades_days：date 之前的全部交易日（含 date）。"""
    cal = _get_calendar()
    if date is not None:
        cal = cal[cal <= pd.Timestamp(date)]
    return [d.to_pydatetime().date() for d in cal]


def get_trading_day_by_date(query_date, day=0):
    """docx get_trading_day_by_date：按日期偏移交易日。"""
    cal = _get_calendar()
    ts = pd.Timestamp(query_date)
    idx = int(cal.searchsorted(ts))
    target = idx + int(day)
    if target < 0:
        target = 0
    if target >= len(cal):
        target = len(cal) - 1
    return cal[target].to_pydatetime().date()


def get_etf_info(stocks):
    """docx get_etf_info：{码: 名称}。复用 get_stock_name。"""
    return get_stock_name(stocks)
```

- [ ] **Step 4: 运行本地引擎测试，确认通过**

Run: `cd backend && uv run --extra dev pytest tests/quant/test_ptrade_70978ed5_engine.py -v`
Expected: PASS（若个别用例因格式细节失败，按错误修正实现以匹配断言；断言只检查宽表形态/键存在，不检查具体值）。

- [ ] **Step 5: 实现 rqalpha 引擎 ptradecompat.py 补齐函数**

在 `backend/app/quant/ptradecompat.py` 的 `get_trade_days` 之后追加：

```python
def get_price(security, start_date=None, end_date=None, frequency="1d",
              fields=None, fq="pre", count=None, is_dict=False):
    """docx 原生 get_price：复用 get_history 口径（rqalpha 环境）。返回宽表 DataFrame。"""
    fields = fields or ["close"]
    if isinstance(fields, str):
        fields = [fields]
    if isinstance(security, str):
        codes = [security]
    else:
        codes = list(security)
    field_out = {}
    for f in fields:
        df = get_history(count, frequency, f, security_list=codes, include=False, fq=fq)
        if df is None or df.empty:
            continue
        field_out[f] = df
    if not field_out:
        return pd.DataFrame()
    if len(fields) == 1:
        df = field_out[fields[0]]
        if len(codes) == 1 and len(df.columns) == 1:
            df = df.copy()
            df.columns = [fields[0]]
        return df
    return field_out


def check_limit(security, query_date=None):
    """docx check_limit：{码: int}。回测用日线 high_limit/low_limit + 收盘价推导。"""
    from rqalpha.environment import Environment
    env = Environment.get_instance()
    codes = [security] if isinstance(security, str) else list(security)
    out = {}
    for c in codes:
        out[c] = 0
        try:
            df = get_history(1, "1d", ["high_limit", "low_limit", "close"],
                             security_list=c, include=True)
            if df is None or df.empty:
                continue
            last = float(df["close"].iloc[-1])
            high = float(df["high_limit"].iloc[-1]) if "high_limit" in df.columns else 0
            low = float(df["low_limit"].iloc[-1]) if "low_limit" in df.columns else 0
            if high and last >= high:
                out[c] = 1
            elif low and last <= low:
                out[c] = -1
        except Exception:
            continue
    return out


def get_stock_info(stocks, field=None):
    """docx get_stock_info：{码: {stock_name, ...}}。名称取 _NAMES。"""
    codes = [stocks] if isinstance(stocks, str) else list(stocks)
    fields = ["stock_name", "listed_date", "de_listed_date"] if field is None else (
        [field] if isinstance(field, str) else list(field))
    out = {}
    for c in codes:
        jq = _to_jq(c)
        out[c] = {f: ("" if f != "stock_name" else _NAMES.get(jq, c)) for f in fields}
    return out


def get_snapshot(security):
    """docx get_snapshot：{码: {up_px, down_px, last_px, ...}}。回测回退日线字段。"""
    codes = [security] if isinstance(security, str) else list(security)
    out = {}
    for c in codes:
        snap = {"last_px": 0.0, "up_px": 0.0, "down_px": 0.0,
                "preclose_px": 0.0, "high_px": 0.0, "low_px": 0.0,
                "business_balance": 0.0, "volume": 0, "amount": 0}
        try:
            df = get_history(1, "1d", ["close", "high_limit", "low_limit", "preclose"],
                             security_list=c, include=True)
            if df is not None and not df.empty:
                snap["last_px"] = float(df["close"].iloc[-1])
                if "high_limit" in df.columns:
                    snap["up_px"] = float(df["high_limit"].iloc[-1])
                if "low_limit" in df.columns:
                    snap["down_px"] = float(df["low_limit"].iloc[-1])
                if "preclose" in df.columns:
                    snap["preclose_px"] = float(df["preclose"].iloc[-1])
        except Exception:
            pass
        out[c] = snap
    return out


def order_target(security, amount, limit_price=None):
    cur = get_position(security).amount
    diff = int(amount) - int(cur)
    if diff == 0:
        return True
    return order(security, diff) if diff > 0 else order(security, -min(abs(diff), get_position(security).enable_amount))


def order_target_value(security, value, limit_price=None):
    from rqalpha.environment import Environment
    env = Environment.get_instance()
    jq = _to_jq(security)
    price = float(getattr(env.portfolio, "close_position_prices", {}).get(jq, 0) or 0)
    if price == 0:
        return False
    cur_val = get_position(security).amount * price
    diff = float(value) - cur_val
    if abs(diff) < price * 100:
        return True
    return order_value(security, diff)


def get_all_trades_days(date=None):
    from rqalpha.environment import Environment
    env = Environment.get_instance()
    cal = env.data_proxy.get_trading_calendar()
    if date is not None:
        cal = cal[cal <= pd.Timestamp(date)]
    return [d.to_pydatetime().date() for d in cal]


def get_trading_day_by_date(query_date, day=0):
    from rqalpha.environment import Environment
    env = Environment.get_instance()
    cal = env.data_proxy.get_trading_calendar()
    ts = pd.Timestamp(query_date)
    idx = int(cal.searchsorted(ts))
    target = min(max(idx + int(day), 0), len(cal) - 1)
    return cal[target].to_pydatetime().date()


def get_etf_info(stocks):
    return get_stock_name(stocks)
```

- [ ] **Step 6: 在 `_register_ptrade_apis` 中注册新函数**

在 `backend/app/quant/ptradecompat.py` 的 `_register_ptrade_apis` 中追加（在 `register_api("log", log)` 之后）：

```python
    register_api("get_price", get_price)
    register_api("check_limit", check_limit)
    register_api("get_stock_info", get_stock_info)
    register_api("get_snapshot", get_snapshot)
    register_api("order_target", order_target)
    register_api("order_target_value", order_target_value)
    register_api("get_all_trades_days", get_all_trades_days)
    register_api("get_trading_day_by_date", get_trading_day_by_date)
    register_api("get_etf_info", get_etf_info)
```

- [ ] **Step 7: 在 loader 命名空间注入新函数**

修改 `backend/app/quant/ptradeengine/ptrade_loader.py` 的 `load_strategy` 的 `ns` dict，追加：

```python
        "get_price": ptrade_api.get_price,
        "check_limit": ptrade_api.check_limit,
        "get_stock_info": ptrade_api.get_stock_info,
        "get_snapshot": ptrade_api.get_snapshot,
        "order_target": ptrade_api.order_target,
        "order_target_value": ptrade_api.order_target_value,
        "get_all_trades_days": ptrade_api.get_all_trades_days,
        "get_trading_day_by_date": ptrade_api.get_trading_day_by_date,
        "get_etf_info": ptrade_api.get_etf_info,
```

- [ ] **Step 8: 运行全部 ptrade 引擎相关测试**

Run: `cd backend && uv run --extra dev pytest tests/quant/test_ptrade_70978ed5_engine.py tests/quant/test_ptrade_strategy_file.py tests/quant/test_ptradecompat.py -q`
Expected: PASS。

- [ ] **Step 9: 提交**

```bash
git add backend/app/quant/ptradeengine/ptrade_api.py backend/app/quant/ptradecompat.py backend/app/quant/ptradeengine/ptrade_loader.py backend/tests/quant/test_ptrade_70978ed5_engine.py
git commit -m "feat(quant): ptrade 引擎补齐 docx 原生函数（get_price/check_limit/get_stock_info/get_snapshot/order_target(_value)/get_all_trades_days/get_trading_day_by_date/get_etf_info）+ loader 注入"
```

---

### Task 3: 编写 PTrade 原生策略文件（核心交付物）

**Files:**
- Create: `backend/tests/fixtures/70978ed5_ptrade/70978ed5.ptrade.py`（完整内容见下）

**Interfaces:**
- Consumes: Task 2 引擎新增函数；`70978ed5.py`（JQ 原版）正文逻辑。
- Produces: 完整 PTrade 原生策略（单持仓，holdings_num=1，13:10 同点 ×3，09:40 run_daily，before/after/handle 钩子）。

**规则（机械转换，正文交易逻辑一行不改）：**
1. 头部注释块改为「PTrade 原生移植版」，标注原 JQ 来源路径与平台差异说明（仿照 dual fixture 头部）。
2. 代码全部 `.SS`/`.SZ`（池定义/基准/指数码直接改写，禁止转换函数）。
3. `set_slippage(0.0001)`、`set_commission(commission_ratio=0.0001, min_commission=5.0, type='ETF')`，`set_benchmark("510300.SS")`，外层 try/except 包住（真机仅回测可用）。
4. 删 `set_option`/`set_order_cost`/`log.set_level`/`record(...)`（`record(走弱期状态=...)` 改 `log.info`）。
5. 调度映射：`before_trading_start` 调 `morning_routine`；`run_daily(context, check_weak_period_daily, time='09:40')`；`run_daily(context, afternoon_routine, time='13:10')`（3 个同点，按注册顺序：afternoon→sell→buy）；`after_trading_end` 调 `reset_daily_flags`；`handle_data` 里先 `_capture_bars(data)` 再按分钟窗口调 `minute_level_stop_loss`。
6. 插入原生辅助层（拷贝自 dual fixture 并精简）：`_BARS`/`_capture_bars`/`_current_dt`/`_today`/`_series_values`/`_series_last`/`_wide`/`_positions`/`_price`/`_single_daily`/`_is_halted`/`_limit_prices`/`_refresh_halt_status`/`get_security_name`/`_get_today_volumes`/`_get_money_avg_series`/`_get_all_fund_codes`/`_ensure_fund_universe`/`_is_real_ptrade` 等（完整代码见下方「辅助层代码块」）。
7. 正文替换映射：
   - `g.avg_etf_money_threshold` 相关：原 DataManager `dm.get_daily_money_cached`/`get_price(...,'money')` 改走 `_get_money_avg_series`（分块 get_history 拉 money，200 只/块）。
   - `get_all_securities(['etf'])`/`df_etf.index/display_name` → `_get_all_fund_codes(context)` + `get_security_name`。
   - `attribute_history(code, n, '1d', ['close'])` → `get_history(n, '1d', 'close', security_list=code, fq='pre')` 经 `_wide` 归一后取值。
   - `get_current_data()[c].paused` → `_is_halted(c, context)`。
   - `get_current_data()[c].last_price` → `_price(c, context)`。
   - `get_current_data()[c].high_limit/low_limit` → `_limit_prices(c, context)`。
   - `context.portfolio.positions` / `position.total_amount/avg_cost/closeable_amount` → `_positions()` / `pos.amount/cost_basis/enable_amount`。
   - `context.portfolio.available_cash` → `context.portfolio.cash`；`context.portfolio.total_value` → `context.portfolio.portfolio_value`。
   - `context.current_dt` → `_current_dt(context)`（内部 `context.blotter.current_dt`）。
   - `get_trade_days(start_date, end_date)` → `get_trade_days(start_date=..., end_date=...)`（关键字参数）。
   - `order(sec, amount)` 原样（docx 同名）。
   - 删 `from jqdata import *` 与 `from app.quant.jqengine.datasource.manager import get_data_manager`。
8. 保留 `import numpy as np`/`math`/`pandas as pd`/`datetime` 与 `warnings.filterwarnings("ignore")`。

**辅助层代码块（直接使用，拷贝到辅助函数区）：**

```python
_BARS = {}  # 最新行情快照 {code: SecurityUnitData}，由 handle_data 捕获
_HALT_CACHE = {}
_LIMIT_CACHE = {}
_MONEY_CACHE = {}
_IS_REAL_PTRADE = None


def _current_dt(context):
    try:
        return context.blotter.current_dt
    except Exception:
        return datetime.now()


def _today(context):
    return _current_dt(context).date()


def _capture_bars(data):
    global _BARS
    if data is None:
        return
    try:
        items = data.items()
    except Exception:
        return
    out = {}
    try:
        for k, v in items:
            out[k] = v
    except Exception:
        return
    if out:
        _BARS = out


def _is_real_ptrade():
    """真机判定：本地引擎 get_market_list 返回 mic='ALL'，真机返回 SS/SZ。"""
    global _IS_REAL_PTRADE
    if _IS_REAL_PTRADE is not None:
        return _IS_REAL_PTRADE
    try:
        mdf = get_market_list()
        mics = list(mdf['finance_mic']) if hasattr(mdf, 'columns') else []
        _IS_REAL_PTRADE = not any(m == 'ALL' for m in mics)
    except Exception:
        _IS_REAL_PTRADE = False
    return _IS_REAL_PTRADE


def _series_values(df, security, field):
    if df is None or not hasattr(df, 'columns') or len(df) == 0:
        return None
    if field in df.columns:
        arr = df[field]
    elif security in df.columns:
        arr = df[security]
    elif df.shape[1] >= 1:
        arr = df.iloc[:, 0]
    else:
        return None
    try:
        return np.asarray(pd.to_numeric(arr, errors='coerce'), dtype=float)
    except Exception:
        return None


def _series_last(df, security, field):
    vals = _series_values(df, security, field)
    if vals is None or len(vals) == 0:
        return None
    try:
        v = float(vals[-1])
    except Exception:
        return None
    return v if v == v else None


def _wide(df, value_col=None):
    if df is None or not isinstance(df, pd.DataFrame) or 'code' not in df.columns:
        return df
    vcol = value_col
    if vcol is None:
        for c in ('close', 'volume', 'money'):
            if c in df.columns:
                vcol = c
                break
    if vcol:
        try:
            return df.pivot_table(index=df.index, columns='code', values=vcol)
        except Exception:
            pass
    return df


def _positions():
    out = {}
    try:
        for code, pos in (get_positions() or {}).items():
            if not code:
                continue
            code = str(code).replace('.XSHG', '.SS').replace('.XSHE', '.SZ')
            if getattr(pos, 'amount', 0) > 0:
                out[code] = pos
    except Exception:
        pass
    return out


def _price(security, context):
    obj = _BARS.get(security)
    p = (getattr(obj, 'close', 0) or getattr(obj, 'price', 0) or 0) if obj else 0
    if p:
        return float(p)
    try:
        mdf = get_history(1, '1m', 'close', security_list=security, include=True)
        val = _series_last(mdf, security, 'close')
        if val:
            return val
    except Exception:
        pass
    try:
        ddf = get_history(1, '1d', 'close', security_list=security, include=True)
        val = _series_last(ddf, security, 'close')
        if val:
            return val
    except Exception:
        pass
    return 0.0


def _refresh_halt_status(codes, context):
    global _HALT_CACHE
    today = _today(context).strftime('%Y%m%d')
    if today not in _HALT_CACHE:
        result = {}
        CHUNK = 100
        cl = list(codes)
        for i in range(0, len(cl), CHUNK):
            try:
                res = get_stock_status(cl[i:i + CHUNK], query_type='HALT', query_date=today)
                if res:
                    result.update(res)
            except Exception:
                continue
        _HALT_CACHE[today] = result
    return _HALT_CACHE[today]


def _is_halted(code, context):
    try:
        m = _HALT_CACHE.get(_today(context).strftime('%Y%m%d'))
        if m is None:
            m = _refresh_halt_status([code], context)
        return bool(m.get(code))
    except Exception:
        return False


def _single_daily(code, field, context):
    try:
        df = get_history(1, '1d', field, security_list=code, include=True)
        v = _series_last(df, code, field)
        if v is not None:
            return v
    except Exception:
        pass
    return None


def _limit_prices(code, context):
    today = _today(context).strftime('%Y%m%d')
    key = (today, code)
    if key in _LIMIT_CACHE:
        return _LIMIT_CACHE[key]
    high = _single_daily(code, 'high_limit', context)
    low = _single_daily(code, 'low_limit', context)
    _LIMIT_CACHE[key] = (high, low)
    return (high, low)


def get_security_name(security):
    try:
        if getattr(g, 'etf_names_dict', {}) and security in g.etf_names_dict:
            return g.etf_names_dict[security]
        d = get_stock_name(security)
        if d and d.get(security):
            return d.get(security)
    except Exception:
        pass
    return security


def _get_today_volumes(context, codes):
    out = {}
    today = _today(context)
    CHUNK = 200
    cl = list(codes)
    for i in range(0, len(cl), CHUNK):
        chunk = cl[i:i + CHUNK]
        try:
            mdf = _wide(get_history(241, '1m', 'volume', security_list=chunk, include=True), 'volume')
            if mdf is None or mdf.empty:
                continue
            for code in chunk:
                if code not in mdf.columns:
                    continue
                s = mdf[code]
                if hasattr(mdf.index, 'date'):
                    s = s[mdf.index.date == today]
                s = pd.to_numeric(s, errors='coerce').dropna()
                out[code] = float(s.sum())
        except Exception:
            continue
    return out


def _resolve_money_field(field):
    if field == 'money_corrected' and _is_real_ptrade():
        return 'money'
    return field


def _get_money_avg_series(codes, count, context, field='money'):
    field = _resolve_money_field(field)
    day = _today(context)
    key = (day, count, field)
    cached = _MONEY_CACHE.get(key)
    if cached is not None:
        sel = cached.reindex([c for c in codes if c in cached.index])
        if len(sel) >= len(codes):
            return sel
    result = pd.Series(dtype=float)
    CHUNK = 200
    cl = list(codes)
    for i in range(0, len(cl), CHUNK):
        chunk = cl[i:i + CHUNK]
        try:
            df = _wide(get_history(count, '1d', field, security_list=chunk), field)
            if df is None or df.empty:
                continue
            df = df.fillna(0.0)
            avg = df.sum(axis=0) / count
            for code in chunk:
                if code in avg.index:
                    result[code] = float(avg[code])
        except Exception:
            continue
    if cached is None or len(result) >= len(cached):
        _MONEY_CACHE[key] = result
    return result


def _get_all_fund_codes(context):
    """全市场基金代码（PTrade 码）。本地引擎 get_etf_list；真机 get_market_list/get_market_detail。"""
    codes = []
    try:
        etf_list = get_etf_list()
        if etf_list:
            return [str(c) for c in etf_list]
    except Exception:
        pass
    try:
        mdf = get_market_list()
        if mdf is not None and hasattr(mdf, 'columns'):
            for mic in mdf['finance_mic']:
                try:
                    d = get_market_detail(mic)
                    if d is not None and hasattr(d, 'columns') and 'prod_code' in d.columns:
                        for c in d['prod_code']:
                            c = str(c)
                            num = c.split('.')[0]
                            if num.startswith(('5', '15', '16')):
                                codes.append(c)
                except Exception:
                    continue
    except Exception:
        pass
    return codes
```

**正文转换后的关键函数占位说明**（正文其余函数按规则 7 逐一替换；下面列出必须保留/改写的函数签名清单，按 70978ed5.py 原顺序）：

`initialize(context)`、`check_weak_period_daily(context)`、`morning_routine(context)`、`midday_routine(context)`、`afternoon_routine(context)`、`sell_routine(context)`、`buy_routine(context)`、`reset_daily_flags(context)`、`check_positions(context)`、`monitor_drawdown(context)`、`calculate_global_etf_threshold(context)`、`filter_global_pool_by_volume(context)`、`update_sector_pool(context)`、`filter_fixed_pool_by_volume(context)`、`daily_merge_etf_pools(context)`、`calculate_and_log_ranked_etfs(context)`、`calculate_momentum_score`、`calculate_all_metrics_for_etf`、`get_volume_ratio`、`check_a_share_weak_period(context)`、`apply_filters`、`get_final_ranked_etfs(context)`、`execute_sell_trades(context)`、`execute_buy_trades(context)`、`is_temporarily_suspended(security, context)`、`smart_order_target_value(security, target_value, context)`、`minute_level_stop_loss(context)`、`check_defensive_etf_available(context)`、`trade(context)`、`before_trading_start(context, data)`、`after_trading_end(context, data)`、`handle_data(context, data)`。

- [ ] **Step 1: 编写完整 PTrade 原生策略文件**

将上述辅助层 + 规则 1-8 应用于 `backend/tests/fixtures/70978ed5_ptrade/70978ed5.py` 生成完整 `backend/tests/fixtures/70978ed5_ptrade/70978ed5.ptrade.py`（参考 `backend/tests/fixtures/dual_v54/wufu-v5.4-dual-adapt.ptrade.py` 的写法；holdings_num 保持 1、13:10 同点、日志文案保留 70978ed5 原版）。

- [ ] **Step 2: 运行文件验证测试**

Run: `cd backend && uv run --extra dev pytest tests/quant/test_ptrade_70978ed5_file.py -v`
Expected: 全部 PASS（编译、无 jq API、调度断言、run_daily 数量、13:10 同点）。

- [ ] **Step 3: 本地引擎加载冒烟（模拟盘 runner 加载路径）**

Run: `cd backend && uv run --extra dev python -c "
from app.quant.ptradeengine import ptrade_api, ptrade_loader
code = open('tests/fixtures/70978ed5_ptrade/70978ed5.ptrade.py', encoding='utf-8').read()
class M:
    def fetch(self, *a, **k): return None
    def get_minute_price_at(self, *a, **k): return 0.0
    def get_daily_money_cached(self, *a, **k): return None
bundle = ptrade_loader.load_strategy(code, M(), 0.0001, 0.0001, 100000.0)
assert bundle.init_fn is not None
assert len(bundle.daily) == 4
print('daily tasks:', [t[1] for t in bundle.daily])
print('OK')
"
Expected: `daily tasks: ['09:40', '13:10', '13:10', '13:10']` 和 `OK`。

- [ ] **Step 4: 提交**

```bash
git add backend/tests/fixtures/70978ed5_ptrade/70978ed5.ptrade.py
git commit -m "feat(strategy): 70978ed5 PTrade 原生移植版（严格按 guojin-ptrade-api.docx，单持仓/13:10 同点）"
```

---

### Task 4: 注册策略库条目 + 回测路由验证

**Files:**
- Modify: 无（只调用 store 公开 API）
- Test: `backend/tests/quant/test_ptrade_70978ed5_register.py`

**Interfaces:**
- Consumes: `app.quant.strategies.store.import_strategy(name, code)`。
- Produces: 新策略 sid（uuid hex[:8]，运行时确定，测试打印记录），`data/quant_strategies/{newsid}.py` 落盘。

- [ ] **Step 1: 写注册测试**

创建 `backend/tests/quant/test_ptrade_70978ed5_register.py`：

```python
"""70978ed5 ptrade 版注册进策略库：import_strategy 落盘 + 可被回测路由识别。"""
from pathlib import Path

from app.quant.strategies import store

PT_FIXTURE = Path(__file__).parent.parent / "fixtures" / "70978ed5_ptrade" / "70978ed5.ptrade.py"


def test_register_ptrade_strategy():
    code = PT_FIXTURE.read_text(encoding="utf-8")
    assert ".SS" in code
    sid = store.import_strategy("五福v5.4-ptrade", code)
    assert len(sid) == 8
    saved = store.get_strategy(sid)
    assert saved is not None
    assert saved["name"] == "五福v5.4-ptrade"
    assert ".SS" in saved["code"]
    p = Path(__file__).parent.parent.parent / "data" / "quant_strategies" / f"{sid}.py"
    assert p.exists()
    print(f"registered sid={sid} file={p}")
```

- [ ] **Step 2: 运行注册测试**

Run: `cd backend && uv run --extra dev pytest tests/quant/test_ptrade_70978ed5_register.py -v`
Expected: PASS 并打印新 sid。

- [ ] **Step 3: 验证回测路由（`_looks_like_ptrade`）**

Run: `cd backend && uv run --extra dev python -c "
import sys, os
sys.path.insert(0, os.getcwd())
from app.quant.strategies import store
sids = store.list_strategies()
rows = [s for s in sids if s['name'] == '五福v5.4-ptrade']
assert rows, '未注册'
sid = rows[0]['id']
code = store.get_strategy(sid)['code']
assert '.SS' in code and '.SZ' in code, 'ptrade 策略应含 .SS/.SZ 码'
from scripts.run_quant_backtest import _looks_like_ptrade
assert _looks_like_ptrade(code), '路由判定失败'
print('route OK sid=', sid)
"
`
Expected: `route OK sid=xxxxxxxx`

- [ ] **Step 4: 提交（注册动作本身不提交，仅提交测试与说明；注册结果在运行环境生效）**

```bash
git add backend/tests/quant/test_ptrade_70978ed5_register.py
git commit -m "test: 70978ed5 ptrade 版注册与回测路由验证"
```

---

### Task 5: 对齐门禁（rqalpha 全窗口 JQ vs PTrade）

**Files:**
- Create: `backend/tests/quant/test_70978ed5_ptrade_alignment.py`
- 依赖：`backend/scripts/run_jq_rqalpha.py`、`backend/scripts/run_ptrade_rqalpha.py`、Task 2 引擎补齐。

**Interfaces:**
- Consumes: `JQ_SOURCE` / `PT_STRATEGY`（Task 1 定义）；rqalpha 两个 runner 脚本。
- Produces: 对齐门禁测试（integration 标记）。

- [ ] **Step 1: 写对齐门禁测试**

创建 `backend/tests/quant/test_70978ed5_ptrade_alignment.py`：

```python
"""70978ed5 ptrade vs jq rqalpha 回测对齐：成交一致 + 逐日净值零差。"""
import os
import subprocess
import sys
from pathlib import Path

import pandas as pd
import pytest

BACKEND = Path(__file__).parent.parent.parent
JQ_RUNNER = BACKEND / "scripts" / "run_jq_rqalpha.py"
PT_RUNNER = BACKEND / "scripts" / "run_ptrade_rqalpha.py"
FIXTURE = BACKEND / "tests" / "fixtures" / "70978ed5_ptrade"
JQ = FIXTURE / "70978ed5.py"
PT = FIXTURE / "70978ed5.ptrade.py"
START, END, CASH, FEE, SLIP = "2026-04-01", "2026-07-16", "100000", "0.0001", "0.0001"


@pytest.mark.integration
@pytest.mark.skipif(not JQ.exists() or not PT.exists(), reason="fixture 缺失")
def test_full_window_alignment(tmp_path):
    jq_out = tmp_path / "jq"
    pt_out = tmp_path / "pt"
    env = dict(os.environ, PYTHONPATH=str(BACKEND))
    subprocess.run([sys.executable, str(JQ_RUNNER), "--start", START, "--end", END,
                    "--strategy", str(JQ), "--out", str(jq_out),
                    "--cash", CASH, "--fee", FEE, "--slippage", SLIP],
                   env=env, check=True, capture_output=True, timeout=3000)
    subprocess.run([sys.executable, str(PT_RUNNER), "--start", START, "--end", END,
                    "--strategy", str(PT), "--out", str(pt_out),
                    "--cash", CASH, "--fee", FEE, "--slippage", SLIP],
                   env=env, check=True, capture_output=True, timeout=3000)

    jq_tr = pd.read_csv(jq_out / "trades.csv")
    pt_tr = pd.read_csv(pt_out / "trades.csv")
    pd.testing.assert_frame_equal(jq_tr, pt_tr)

    jq_eq = pd.read_csv(jq_out / "equity.csv")
    pt_eq = pd.read_csv(pt_out / "equity.csv")
    m = pd.merge(jq_eq, pt_eq, on="date", suffixes=("_jq", "_pt"))
    assert len(m) > 20
    diff = (m["value_pt"] - m["value_jq"]).abs()
    assert diff.max() == 0.0, f"逐日净值存在差异: max={diff.max():.6f}"
```

- [ ] **Step 2: 运行对齐门禁（需本地 stock data 服务在跑）**

Run: `cd backend && uv run --extra dev pytest -m integration tests/quant/test_70978ed5_ptrade_alignment.py -q`
Expected: PASS（两条 runner 子进程各跑全窗口约 2-10 分钟，断言成交一致 + 净值零差）。

- [ ] **Step 3: 若对齐失败，定位差异并修复策略文件后重跑**

常见差异源与修法：
- get_history 宽表列名/切片差异 → 检查 `_series_values`/`_wide` 归一。
- 日线 end_date 边界（盘中 <15:00 回退前一交易日）→ 引擎已处理，检查 `get_price` 是否透传 end_date。
- 分钟量求和口径（241 根是否含当前 bar）→ 对齐 `include` 参数。
- 日志差异不影响成交；若成交差 1 手（整手尾差）→ 检查 `order_target_value`/`smart_order_target_value` 的整手取整逻辑是否与 jq 版 `int(target_value / estimated_price) // 100 * 100` 一致。

- [ ] **Step 4: 提交**

```bash
git add backend/tests/quant/test_70978ed5_ptrade_alignment.py
git commit -m "test: 70978ed5 ptrade vs jq rqalpha 全窗口对齐门禁（成交一致 + 净值零差）"
```

---

### Task 6: 全量校验与收尾

**Files:**
- Modify: 视 Task 5 修复需要。
- 运行：ruff / mypy / 全量单测 / 冒烟回测。

- [ ] **Step 1: 全量单测**

Run: `cd backend && uv run --extra dev pytest tests/quant -q`
Expected: PASS（含新老 ptrade 测试）。

- [ ] **Step 2: ruff + mypy**

Run: `cd backend && uv run --extra dev ruff check app tests && uv run --extra dev mypy app`
Expected: 无报错（新代码符合 line-length 100、E/F/I/N/UP/B/SIM/RUF 规则）。

- [ ] **Step 3: 冒烟回测**

Run: `cd backend && uv run --extra dev python scripts/run_ptrade_rqalpha.py --strategy tests/fixtures/70978ed5_ptrade/70978ed5.ptrade.py --start 2026-07-10 --end 2026-07-16 --out /tmp/opencode/ptrade_70978ed5_smoke`
Expected: 脚本正常退出，`/tmp/opencode/ptrade_70978ed5_smoke/trades.csv` 与 `equity.csv` 生成。

- [ ] **Step 4: 确认 JQ 原版未改动**

Run: `git status --short data/quant_strategies/70978ed5.py`
Expected: 无输出（未改动）。

- [ ] **Step 5: 提交收尾**

```bash
git add -A
git commit -m "chore: 70978ed5 ptrade 移植收尾（校验通过）" || true
```

---

## Self-Review Checklist

- **Spec coverage:**
  - §1 交付物（fixture + 策略库条目 + 引擎 + 测试 + 文档）→ Task 1/3/4/2/5/6。
  - §2 映射表（全部 jq 独有 API 替换 + 调度 + 数据访问）→ Task 3 规则 1-8。
  - §3 引擎扩展（9 个 docx 函数 + 格式保证）→ Task 2。
  - §4 调度（13:10 同点 ×3、run_daily ≤5）→ Task 3 规则 5 + Task 1 测试断言。
  - §5 对齐门禁（全窗口成交一致 + 净值零差 + 编译测试 + 冒烟）→ Task 1/5/6。
  - §6 风险（同点时序/返回形态/性能/范围收口）→ 已知风险记录，Task 5 覆盖本地对齐。
- **Placeholder scan:** 无 TBD/TODO；辅助层与测试代码均完整给出。
- **Type consistency:** 引擎函数签名在 Task 2 定义（`get_price(security, start_date, end_date, frequency, fields, fq, count, is_dict)` 等），Task 3 策略按此调用；`_is_halted/_limit_prices/_price/_positions/_get_money_avg_series/_get_all_fund_codes` 在 Task 3 定义并被正文引用，命名一致。